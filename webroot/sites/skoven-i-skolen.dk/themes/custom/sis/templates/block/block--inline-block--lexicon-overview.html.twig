{{ attach_library('sis/lexicon') }}
<div{{ attributes.addClass('lexicon') }}>
  {{ title_prefix }}
  {{ title_suffix }}
  <div>
    <div class="row">
      {{ filters }}
      {% block content %}
        {{ content }}
      {% endblock %}
    </div>
  </div>
</div>
